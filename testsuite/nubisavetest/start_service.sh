#!/bin/bash
if [ $# -gt 0 ]; then
   echo
   echo "Too many arguments."
   echo
   echo
   echo Currently does nothing. Nubisave needs to be started and configured manually before running the test.
   echo Usage: `basename $0`
   echo Example: `basename $0`
   echo
   exit
fi  

sleep 10
